node('master') {
    // some block
}


stage('scm') {
    // some block
}

stage('build') {
    // some block
}

withMaven(jdk: 'default Java', maven: 'Default Maven') {
    // some block
}

sh label: '', script: 'mvn clean install'


